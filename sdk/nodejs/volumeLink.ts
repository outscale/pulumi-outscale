// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Manages a volume link.
 *
 * For more information on this resource, see the [User Guide](https://docs.outscale.com/en/userguide/About-Volumes.html).\
 * For more information on this resource actions, see the [API documentation](https://docs.outscale.com/api#3ds-outscale-api-volume).
 *
 * ## Example Usage
 *
 * ### Required resources
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as outscale from "@outscale/pulumi-outscale";
 *
 * const volume01 = new outscale.Volume("volume01", {
 *     subregionName: `${region}a`,
 *     size: 40,
 * });
 * const vm01 = new outscale.Vm("vm01", {
 *     imageId: imageId,
 *     vmType: vmType,
 *     keypairName: keypairName,
 *     securityGroupIds: [securityGroupId],
 * });
 * ```
 *
 * ### Link a volume to a VM
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as outscale from "@outscale/pulumi-outscale";
 *
 * const volumeLink01 = new outscale.VolumeLink("volume_link01", {
 *     deviceName: "/dev/xvdc",
 *     volumeId: volume01.id,
 *     vmId: vm01.id,
 * });
 * ```
 *
 * ## Import
 *
 * A volume link can be imported using a volume ID. For example:
 *
 * console
 *
 * ```sh
 * $ pulumi import outscale:index/volumeLink:VolumeLink ImportedVolumeLink vol-12345678
 * ```
 */
export class VolumeLink extends pulumi.CustomResource {
    /**
     * Get an existing VolumeLink resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: VolumeLinkState, opts?: pulumi.CustomResourceOptions): VolumeLink {
        return new VolumeLink(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'outscale:index/volumeLink:VolumeLink';

    /**
     * Returns true if the given object is an instance of VolumeLink.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is VolumeLink {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === VolumeLink.__pulumiType;
    }

    /**
     * If true, the volume is deleted when terminating the VM. If false, the volume is not deleted when terminating the VM.
     */
    public /*out*/ readonly deleteOnVmDeletion!: pulumi.Output<boolean>;
    /**
     * The name of the device. For a root device, you must use `/dev/sda1`. For other volumes, you must use `/dev/sdX`, `/dev/sdXX`, `/dev/xvdX`, or `/dev/xvdXX` (where the first `X` is a letter between `b` and `z`, and the second `X` is a letter between `a` and `z`).
     */
    public readonly deviceName!: pulumi.Output<string>;
    public readonly forceUnlink!: pulumi.Output<boolean>;
    public /*out*/ readonly requestId!: pulumi.Output<string>;
    /**
     * The state of the attachment of the volume (`attaching` | `detaching` | `attached` | `detached`).
     */
    public /*out*/ readonly state!: pulumi.Output<string>;
    public readonly timeouts!: pulumi.Output<outputs.VolumeLinkTimeouts | undefined>;
    /**
     * The ID of the VM you want to attach the volume to.
     */
    public readonly vmId!: pulumi.Output<string>;
    /**
     * The ID of the volume you want to attach.
     */
    public readonly volumeId!: pulumi.Output<string>;

    /**
     * Create a VolumeLink resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: VolumeLinkArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: VolumeLinkArgs | VolumeLinkState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as VolumeLinkState | undefined;
            resourceInputs["deleteOnVmDeletion"] = state ? state.deleteOnVmDeletion : undefined;
            resourceInputs["deviceName"] = state ? state.deviceName : undefined;
            resourceInputs["forceUnlink"] = state ? state.forceUnlink : undefined;
            resourceInputs["requestId"] = state ? state.requestId : undefined;
            resourceInputs["state"] = state ? state.state : undefined;
            resourceInputs["timeouts"] = state ? state.timeouts : undefined;
            resourceInputs["vmId"] = state ? state.vmId : undefined;
            resourceInputs["volumeId"] = state ? state.volumeId : undefined;
        } else {
            const args = argsOrState as VolumeLinkArgs | undefined;
            if ((!args || args.deviceName === undefined) && !opts.urn) {
                throw new Error("Missing required property 'deviceName'");
            }
            if ((!args || args.vmId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'vmId'");
            }
            if ((!args || args.volumeId === undefined) && !opts.urn) {
                throw new Error("Missing required property 'volumeId'");
            }
            resourceInputs["deviceName"] = args ? args.deviceName : undefined;
            resourceInputs["forceUnlink"] = args ? args.forceUnlink : undefined;
            resourceInputs["timeouts"] = args ? args.timeouts : undefined;
            resourceInputs["vmId"] = args ? args.vmId : undefined;
            resourceInputs["volumeId"] = args ? args.volumeId : undefined;
            resourceInputs["deleteOnVmDeletion"] = undefined /*out*/;
            resourceInputs["requestId"] = undefined /*out*/;
            resourceInputs["state"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(VolumeLink.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering VolumeLink resources.
 */
export interface VolumeLinkState {
    /**
     * If true, the volume is deleted when terminating the VM. If false, the volume is not deleted when terminating the VM.
     */
    deleteOnVmDeletion?: pulumi.Input<boolean>;
    /**
     * The name of the device. For a root device, you must use `/dev/sda1`. For other volumes, you must use `/dev/sdX`, `/dev/sdXX`, `/dev/xvdX`, or `/dev/xvdXX` (where the first `X` is a letter between `b` and `z`, and the second `X` is a letter between `a` and `z`).
     */
    deviceName?: pulumi.Input<string>;
    forceUnlink?: pulumi.Input<boolean>;
    requestId?: pulumi.Input<string>;
    /**
     * The state of the attachment of the volume (`attaching` | `detaching` | `attached` | `detached`).
     */
    state?: pulumi.Input<string>;
    timeouts?: pulumi.Input<inputs.VolumeLinkTimeouts>;
    /**
     * The ID of the VM you want to attach the volume to.
     */
    vmId?: pulumi.Input<string>;
    /**
     * The ID of the volume you want to attach.
     */
    volumeId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a VolumeLink resource.
 */
export interface VolumeLinkArgs {
    /**
     * The name of the device. For a root device, you must use `/dev/sda1`. For other volumes, you must use `/dev/sdX`, `/dev/sdXX`, `/dev/xvdX`, or `/dev/xvdXX` (where the first `X` is a letter between `b` and `z`, and the second `X` is a letter between `a` and `z`).
     */
    deviceName: pulumi.Input<string>;
    forceUnlink?: pulumi.Input<boolean>;
    timeouts?: pulumi.Input<inputs.VolumeLinkTimeouts>;
    /**
     * The ID of the VM you want to attach the volume to.
     */
    vmId: pulumi.Input<string>;
    /**
     * The ID of the volume you want to attach.
     */
    volumeId: pulumi.Input<string>;
}
