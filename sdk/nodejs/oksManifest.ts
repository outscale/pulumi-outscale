// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Applies a generic Kubernetes manifest on an OKS cluster using the cluster's kubeconfig.
 *
 * The manifest can be written from a Custom Resource Definition (CRD) retrieved from the Kubernetes API, or from the templates returned by the outscale.getOksCrdTemplates data source. Both cluster-scoped and namescape-scoped CRDs are supported.
 *
 * This uses server-side apply (SSA) for each Kubernetes apply/validation call. Create and update operations use patch SSA.
 *
 * > **Note:** This resource is a smaller implementation of what the kubernetesManifest and the kubectlManifest resources offer, that integrates natively with an OKS cluster without needing to initialize another Terraform provider.
 *
 * ## Example Usage
 *
 * ### Required resources
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as outscale from "@outscale/pulumi-outscale";
 *
 * const project = new outscale.OksProject("project", {
 *     name: "project01",
 *     cidr: "10.50.0.0/18",
 *     region: "eu-west-2",
 * });
 * const cluster = new outscale.OksCluster("cluster", {
 *     projectId: project.id,
 *     adminWhitelists: ["0.0.0.0/0"],
 *     cidrPods: "10.91.0.0/16",
 *     cidrService: "10.92.0.0/16",
 *     version: "1.35",
 *     name: "cluster01",
 *     controlPlanes: "cp.mono.master",
 * });
 * ```
 *
 * ### Create an OKS node pool
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as outscale from "@outscale/pulumi-outscale";
 *
 * const nodepool = new outscale.OksManifest("nodepool", {
 *     clusterId: cluster.id,
 *     manifest: `apiVersion: oks.dev/v1beta2
 * kind: NodePool
 * metadata:
 *   name: pool-1
 * spec:
 *   autoHealing: true
 *   desiredNodes: 1
 *   nodeType: tinav7.c1r1p1
 *   upgradeStrategy:
 *     autoUpgradeEnabled: true
 *     autoUpgradeMaintenance:
 *       durationHours: 1
 *       startHour: 12
 *       weekDay: Tue
 *     maxSurge: 0
 *     maxUnavailable: 1
 *   volumes:
 *   - device: root
 *     dir: /
 *     size: 100
 *     type: gp2
 *   zones: [eu-west-2a]
 * `,
 *     waitFor: {
 *         fields: {
 *             "status.progress.ready": "1",
 *         },
 *     },
 * });
 * ```
 */
export class OksManifest extends pulumi.CustomResource {
    /**
     * Get an existing OksManifest resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: OksManifestState, opts?: pulumi.CustomResourceOptions): OksManifest {
        return new OksManifest(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'outscale:index/oksManifest:OksManifest';

    /**
     * Returns true if the given object is an instance of OksManifest.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is OksManifest {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === OksManifest.__pulumiType;
    }

    /**
     * The ID of the cluster on which you want to apply the manifest.
     */
    declare public readonly clusterId: pulumi.Output<string>;
    /**
     * The Kubernetes YAML manifest.
     */
    declare public readonly manifest: pulumi.Output<string>;
    /**
     * The applied Kubernetes object returned as YAML.
     */
    declare public /*out*/ readonly object: pulumi.Output<string>;
    /**
     * If set to true, Terraform removes only the manifest resource from the state during delete, without deleting the Kubernetes object from the cluster. By default, false.
     */
    declare public readonly skipDelete: pulumi.Output<boolean>;
    declare public readonly timeouts: pulumi.Output<outputs.OksManifestTimeouts | undefined>;
    /**
     * : If set to true, Terraform waits for the Kubernetes object to be deleted during destroy. By default, false.
     */
    declare public readonly wait: pulumi.Output<boolean>;
    /**
     * Wait until the fields in the Kubernetes object match the expected values after apply:
     */
    declare public readonly waitFor: pulumi.Output<outputs.OksManifestWaitFor | undefined>;

    /**
     * Create a OksManifest resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: OksManifestArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: OksManifestArgs | OksManifestState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as OksManifestState | undefined;
            resourceInputs["clusterId"] = state?.clusterId;
            resourceInputs["manifest"] = state?.manifest;
            resourceInputs["object"] = state?.object;
            resourceInputs["skipDelete"] = state?.skipDelete;
            resourceInputs["timeouts"] = state?.timeouts;
            resourceInputs["wait"] = state?.wait;
            resourceInputs["waitFor"] = state?.waitFor;
        } else {
            const args = argsOrState as OksManifestArgs | undefined;
            if (args?.clusterId === undefined && !opts.urn) {
                throw new Error("Missing required property 'clusterId'");
            }
            if (args?.manifest === undefined && !opts.urn) {
                throw new Error("Missing required property 'manifest'");
            }
            resourceInputs["clusterId"] = args?.clusterId;
            resourceInputs["manifest"] = args?.manifest;
            resourceInputs["skipDelete"] = args?.skipDelete;
            resourceInputs["timeouts"] = args?.timeouts;
            resourceInputs["wait"] = args?.wait;
            resourceInputs["waitFor"] = args?.waitFor;
            resourceInputs["object"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(OksManifest.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering OksManifest resources.
 */
export interface OksManifestState {
    /**
     * The ID of the cluster on which you want to apply the manifest.
     */
    clusterId?: pulumi.Input<string>;
    /**
     * The Kubernetes YAML manifest.
     */
    manifest?: pulumi.Input<string>;
    /**
     * The applied Kubernetes object returned as YAML.
     */
    object?: pulumi.Input<string>;
    /**
     * If set to true, Terraform removes only the manifest resource from the state during delete, without deleting the Kubernetes object from the cluster. By default, false.
     */
    skipDelete?: pulumi.Input<boolean>;
    timeouts?: pulumi.Input<inputs.OksManifestTimeouts>;
    /**
     * : If set to true, Terraform waits for the Kubernetes object to be deleted during destroy. By default, false.
     */
    wait?: pulumi.Input<boolean>;
    /**
     * Wait until the fields in the Kubernetes object match the expected values after apply:
     */
    waitFor?: pulumi.Input<inputs.OksManifestWaitFor>;
}

/**
 * The set of arguments for constructing a OksManifest resource.
 */
export interface OksManifestArgs {
    /**
     * The ID of the cluster on which you want to apply the manifest.
     */
    clusterId: pulumi.Input<string>;
    /**
     * The Kubernetes YAML manifest.
     */
    manifest: pulumi.Input<string>;
    /**
     * If set to true, Terraform removes only the manifest resource from the state during delete, without deleting the Kubernetes object from the cluster. By default, false.
     */
    skipDelete?: pulumi.Input<boolean>;
    timeouts?: pulumi.Input<inputs.OksManifestTimeouts>;
    /**
     * : If set to true, Terraform waits for the Kubernetes object to be deleted during destroy. By default, false.
     */
    wait?: pulumi.Input<boolean>;
    /**
     * Wait until the fields in the Kubernetes object match the expected values after apply:
     */
    waitFor?: pulumi.Input<inputs.OksManifestWaitFor>;
}
