// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Manages a NAT service.
 *
 * For more information on this resource, see the [User Guide](https://docs.outscale.com/en/userguide/About-NAT-Services.html).\
 * For more information on this resource actions, see the [API documentation](https://docs.outscale.com/api#3ds-outscale-api-natservice).
 *
 * ## Example Usage
 *
 * ### Required resources
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as outscale from "@outscale/pulumi-outscale";
 *
 * const net01 = new outscale.Net("net01", {ipRange: "10.0.0.0/16"});
 * const subnet01 = new outscale.Subnet("subnet01", {
 *     netId: net01.netId,
 *     ipRange: "10.0.0.0/18",
 * });
 * const routeTable01 = new outscale.RouteTable("route_table01", {netId: net01.netId});
 * const outscaleRouteTableLink01 = new outscale.RouteTableLink("outscale_route_table_link01", {
 *     subnetId: subnet01.subnetId,
 *     routeTableId: routeTable01.routeTableId,
 * });
 * const internetService01 = new outscale.InternetService("internet_service01", {});
 * const internetServiceLink01 = new outscale.InternetServiceLink("internet_service_link01", {
 *     netId: net01.netId,
 *     internetServiceId: internetService01.internetServiceId,
 * });
 * const route01 = new outscale.Route("route01", {
 *     destinationIpRange: "0.0.0.0/0",
 *     gatewayId: internetService01.internetServiceId,
 *     routeTableId: routeTable01.routeTableId,
 * }, {
 *     dependsOn: [internetServiceLink01],
 * });
 * const publicIp01 = new outscale.PublicIp("public_ip01", {});
 * ```
 *
 * ### Create a NAT service
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as outscale from "@outscale/pulumi-outscale";
 *
 * const natService01 = new outscale.NatService("nat_service01", {
 *     subnetId: subnet01.subnetId,
 *     publicIpId: publicIp01.publicIpId,
 * }, {
 *     dependsOn: [route01],
 * });
 * ```
 *
 * ## Import
 *
 * A NAT service can be imported using its ID. For example:
 *
 * console
 *
 * ```sh
 * $ pulumi import outscale:index/natService:NatService ImportedNatService nat-87654321
 * ```
 */
export class NatService extends pulumi.CustomResource {
    /**
     * Get an existing NatService resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: NatServiceState, opts?: pulumi.CustomResourceOptions): NatService {
        return new NatService(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'outscale:index/natService:NatService';

    /**
     * Returns true if the given object is an instance of NatService.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is NatService {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === NatService.__pulumiType;
    }

    /**
     * The ID of the NAT service.
     */
    declare public /*out*/ readonly natServiceId: pulumi.Output<string>;
    /**
     * The ID of the Net in which the NAT service is.
     */
    declare public /*out*/ readonly netId: pulumi.Output<string>;
    /**
     * The allocation ID of the public IP to associate with the NAT service.<br />
     * If the public IP is already associated with another resource, you must first disassociate it.
     */
    declare public readonly publicIpId: pulumi.Output<string>;
    /**
     * Information about the public IP or IPs associated with the NAT service.
     */
    declare public /*out*/ readonly publicIps: pulumi.Output<outputs.NatServicePublicIp[]>;
    declare public /*out*/ readonly requestId: pulumi.Output<string>;
    /**
     * The state of the NAT service (`pending` \| `available` \| `deleting` \| `deleted`).
     */
    declare public /*out*/ readonly state: pulumi.Output<string>;
    /**
     * The ID of the Subnet in which you want to create the NAT service.
     */
    declare public readonly subnetId: pulumi.Output<string>;
    /**
     * A tag to add to this resource. You can specify this argument several times.
     */
    declare public readonly tags: pulumi.Output<outputs.NatServiceTag[] | undefined>;

    /**
     * Create a NatService resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: NatServiceArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: NatServiceArgs | NatServiceState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as NatServiceState | undefined;
            resourceInputs["natServiceId"] = state?.natServiceId;
            resourceInputs["netId"] = state?.netId;
            resourceInputs["publicIpId"] = state?.publicIpId;
            resourceInputs["publicIps"] = state?.publicIps;
            resourceInputs["requestId"] = state?.requestId;
            resourceInputs["state"] = state?.state;
            resourceInputs["subnetId"] = state?.subnetId;
            resourceInputs["tags"] = state?.tags;
        } else {
            const args = argsOrState as NatServiceArgs | undefined;
            if (args?.publicIpId === undefined && !opts.urn) {
                throw new Error("Missing required property 'publicIpId'");
            }
            if (args?.subnetId === undefined && !opts.urn) {
                throw new Error("Missing required property 'subnetId'");
            }
            resourceInputs["publicIpId"] = args?.publicIpId;
            resourceInputs["subnetId"] = args?.subnetId;
            resourceInputs["tags"] = args?.tags;
            resourceInputs["natServiceId"] = undefined /*out*/;
            resourceInputs["netId"] = undefined /*out*/;
            resourceInputs["publicIps"] = undefined /*out*/;
            resourceInputs["requestId"] = undefined /*out*/;
            resourceInputs["state"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(NatService.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering NatService resources.
 */
export interface NatServiceState {
    /**
     * The ID of the NAT service.
     */
    natServiceId?: pulumi.Input<string>;
    /**
     * The ID of the Net in which the NAT service is.
     */
    netId?: pulumi.Input<string>;
    /**
     * The allocation ID of the public IP to associate with the NAT service.<br />
     * If the public IP is already associated with another resource, you must first disassociate it.
     */
    publicIpId?: pulumi.Input<string>;
    /**
     * Information about the public IP or IPs associated with the NAT service.
     */
    publicIps?: pulumi.Input<pulumi.Input<inputs.NatServicePublicIp>[]>;
    requestId?: pulumi.Input<string>;
    /**
     * The state of the NAT service (`pending` \| `available` \| `deleting` \| `deleted`).
     */
    state?: pulumi.Input<string>;
    /**
     * The ID of the Subnet in which you want to create the NAT service.
     */
    subnetId?: pulumi.Input<string>;
    /**
     * A tag to add to this resource. You can specify this argument several times.
     */
    tags?: pulumi.Input<pulumi.Input<inputs.NatServiceTag>[]>;
}

/**
 * The set of arguments for constructing a NatService resource.
 */
export interface NatServiceArgs {
    /**
     * The allocation ID of the public IP to associate with the NAT service.<br />
     * If the public IP is already associated with another resource, you must first disassociate it.
     */
    publicIpId: pulumi.Input<string>;
    /**
     * The ID of the Subnet in which you want to create the NAT service.
     */
    subnetId: pulumi.Input<string>;
    /**
     * A tag to add to this resource. You can specify this argument several times.
     */
    tags?: pulumi.Input<pulumi.Input<inputs.NatServiceTag>[]>;
}
